const sum = (a, b) => a + b;
const minus = (a, b) => a - b;
const multiply = (a, b) => a * b;
const settings = {
    foo: "bar",
    xyz: 12345
};

export {
    sum,
    minus,
    multiply,
    settings
}